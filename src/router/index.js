import React from 'react';
import {
  BrowserRouter,
  Route,
  Switch
} from 'react-router-dom';

import Main from '../components/Main';

export default (
    <BrowserRouter>
        <Main />
	  </BrowserRouter>
);